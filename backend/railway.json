{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT --no-server-header",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "_comment": {
    "root": "Set the Railway service root directory to `backend/` (spec §19).",
    "healthcheck": "/health is liveness only and never touches Supabase, so a database outage cannot trigger a restart loop.",
    "worker": "Service #2 uses the same repo and build, with startCommand `python -m workers.runner` and no healthcheckPath."
  }
}
